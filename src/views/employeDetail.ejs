<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Employee Details</title>
    <script src="https://cdn.tailwindcss.com/3.4.16"></script>
    <script>
      tailwind.config = {
        theme: {
          extend: {
            colors: {
              primary: "#4299e1",
              secondary: "#38a169",
            },
            borderRadius: {
              button: "8px",
            },
          },
        },
      };
    </script>
    <link rel="preconnect" href="https://fonts.googleapis.com" />
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
    <link
      href="https://fonts.googleapis.com/css2?family=Pacifico&family=Inter:wght@300;400;500;600;700&display=swap"
      rel="stylesheet"
    />
    <link
      rel="stylesheet"
      href="https://cdnjs.cloudflare.com/ajax/libs/remixicon/4.6.0/remixicon.min.css"
    />
    <style>
      body {
        font-family: "Inter", sans-serif;
        background: linear-gradient(135deg, #f7f9fc 0%, #edf2f7 100%);
        min-height: 100vh;
      }
      .badge-in {
        background: linear-gradient(135deg, #48bb78 0%, #38a169 100%);
        box-shadow: 0 2px 4px rgba(56, 161, 105, 0.2);
      }
      .badge-out {
        background: linear-gradient(135deg, #f56565 0%, #e53e3e 100%);
        box-shadow: 0 2px 4px rgba(229, 62, 62, 0.2);
      }
      .card-hover {
        transition: all 0.3s ease;
      }
      .card-hover:hover {
        transform: translateY(-4px);
        box-shadow: 0 12px 24px rgba(0, 0, 0, 0.1);
      }
    </style>
  </head>
  <body>
    <div class="container mx-auto py-8 px-4 sm:px-6 lg:px-8">
      <div class="mb-6">
        <a
          href="/employes"
          class="inline-flex items-center px-4 py-2 bg-gray-600 hover:bg-gray-700 text-white rounded-button transition-all duration-300 ease-in-out transform hover:-translate-x-1"
        >
          <i class="ri-arrow-left-line mr-2"></i> Ortga
        </a>
      </div>

      <div class="grid grid-cols-1 lg:grid-cols-3 gap-8 mb-8">
        <div
          class="lg:col-span-2 bg-white rounded-lg shadow-md card-hover p-8 relative overflow-hidden"
        >
          <div
            class="absolute top-0 right-0 w-40 h-40 bg-primary/5 rounded-full -mr-20 -mt-20"
          ></div>
          <div
            class="absolute bottom-0 left-0 w-32 h-32 bg-secondary/5 rounded-full -ml-16 -mb-16"
          ></div>
          <div class="flex flex-col md:flex-row justify-between">
            <div>
              <h2 class="text-2xl font-semibold text-primary mb-4">
                <%= employe.firstname %> <%= employe.lastname %>
              </h2>
              <div class="space-y-2 text-gray-700">
                <p>
                  <i class="ri-mail-line mr-2 text-gray-500"></i
                  ><strong>Email:</strong> <%= employe.email %>
                </p>
                <p>
                  <i class="ri-phone-line mr-2 text-gray-500"></i
                  ><strong>Telefon:</strong> <%= employe.phone %>
                </p>
              </div>
            </div>
            <div class="mt-6 md:mt-0 flex justify-center">
              <div
                class="w-32 h-32 bg-gray-100 rounded-full flex items-center justify-center text-gray-400 overflow-hidden border-4 border-primary/20"
              >
                <i class="ri-user-3-line ri-3x"></i>
              </div>
            </div>
          </div>
        </div>

        <div
          class="lg:col-span-1 bg-gradient-to-br from-primary/5 to-secondary/5 rounded-lg shadow-md p-8 card-hover"
        >
          <div class="bg-white/80 backdrop-blur-sm rounded-lg p-6">
            <h4 class="text-lg font-semibold text-gray-800 text-center mb-6">
              Statistika
            </h4>
            <div class="space-y-4">
              <div
                class="flex justify-between items-center p-3 bg-gray-50 rounded-lg"
              >
                <span class="text-sm text-gray-600">Ish kunlari</span>
                <span class="font-semibold text-gray-800">22 kun</span>
              </div>
              <div
                class="flex justify-between items-center p-3 bg-gray-50 rounded-lg"
              >
                <span class="text-sm text-gray-600">O'rtacha ish vaqti</span>
                <span class="font-semibold text-gray-800">8.5 soat</span>
              </div>
              <div
                class="flex justify-between items-center p-3 bg-gray-50 rounded-lg"
              >
                <span class="text-sm text-gray-600">Kechikishlar</span>
                <span class="font-semibold text-gray-800">2 marta</span>
              </div>
            </div>
          </div>
        </div>
      </div>

      <div class="bg-white rounded-lg shadow-md p-8 card-hover">
        <h3 class="text-xl font-semibold text-secondary mb-4 flex items-center">
          <i class="ri-time-line mr-2"></i>Kirish / Chiqish tarixi
        </h3>

        <% if (logs.length === 0) { %>
        <div class="p-4 bg-yellow-100 text-yellow-800 rounded-lg">
          Ma'lumot topilmadi
        </div>
        <% } else { %>
        <div
          class="overflow-x-auto rounded-lg border border-gray-200 bg-white/80 backdrop-blur-sm"
        >
          <table class="min-w-full divide-y divide-gray-200">
            <thead class="bg-gray-50">
              <tr>
                <th
                  class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider"
                >
                  Status
                </th>
                <th
                  class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider"
                >
                  Sana
                </th>
                <th
                  class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider"
                >
                  Vaqt
                </th>
                <th
                  class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider"
                >
                  Kpp
                </th>
              </tr>
            </thead>
            <tbody class="bg-white divide-y divide-gray-200">
              <% logs.forEach(log => { const date = new Date(log.time); const
              sana = date.toLocaleDateString('uz-UZ'); const vaqt =
              date.toLocaleTimeString('uz-UZ'); %>
              <tr class="hover:bg-gray-50">
                <td class="px-6 py-4 whitespace-nowrap">
                  <span
                    class="<%= log.direction === 'in' ? 'badge-in' : 'badge-out' %> text-white text-xs font-medium px-3 py-1 rounded-full"
                  >
                    <%= log.direction.toUpperCase() === 'IN' ? 'KIRISH' :
                    'CHIQISH' %>
                  </span>
                </td>
                <td class="px-6 py-4 whitespace-nowrap text-sm text-gray-500">
                  <%= sana %>
                </td>
                <td class="px-6 py-4 whitespace-nowrap text-sm text-gray-500">
                  <%= vaqt %>
                </td>
                <td class="px-6 py-4 whitespace-nowrap text-sm text-gray-500">
                  <%=log.kppId %>
                </td>
              </tr>
              <% }) %>
            </tbody>
          </table>
        </div>
        <% } %>
      </div>
    </div>
  </body>
</html>
